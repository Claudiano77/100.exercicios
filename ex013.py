s = float(input('Digite seu salário: '))
d = s * 0.15
ns = s + d
print(f'O seu salário de R${s:.2f} recebe um aumento de 15% R${d:.2f} ficando agora por R${ns:.2f}')


#Guanabara
# s = float(input('Qual o salário: '))
# ns = s + s * 15 / 100
# print(f'O salário de R${s:.2f} com o aumento de 15% fica por R${ns:.2f}')